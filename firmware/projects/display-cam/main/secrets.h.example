#pragma once

// Copy this to secrets.h and fill in, OR run
// `just PROJECT=display-cam provision <client_id>` to pull from Firestore.

#define WIFI_SSID       "your-wifi-ssid"
#define WIFI_PASS       "your-wifi-password"

#define SERVER_URL      "https://your-server.example.com"

#define CLIENT_NAME     "display-cam-1"
#define CLIENT_API_KEY  "your-client-api-key"
