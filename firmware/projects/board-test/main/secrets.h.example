#pragma once

// Copy this file to secrets.h and fill in real values. Board test only
// needs WiFi credentials (for the connectivity test step); it does not
// talk to the server.

#define WIFI_SSID       "your-wifi-ssid"
#define WIFI_PASS       "your-wifi-password"
